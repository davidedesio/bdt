<div class="card bg-secondary shadow">
    <div class="card-header bg-white border-0">
        <div class="row align-items-center">
            <div class="col-8">
                <h3 class="mb-0">{{ 'LEADERBOARDS_PAGE_LISTING_SUBTITLE_1'|trans }}</h3>
            </div>
        </div>
    </div>
    <div class="card-body">
        <table id="done" style="width:100%">
            <thead>
            <tr>
                <td>Posizione</td>
                <td>Nome e Cognome</td>
                <td>Attività eseguite</td>
            </thead>
            <tbody>
                {% for leaderboardDoneUser in leaderboardDoneUsers %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td><b>{{ leaderboardDoneUser.name }} {{ leaderboardDoneUser.surname }}</b></td>
                        <td class="highlight dark"><b>{{ leaderboardDoneUser.done }}</b></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>