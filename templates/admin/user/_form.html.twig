{{ form_start(form) }}



    {% include('admin/commons/offcanvas-menu-right.html.twig') %}
    <div id="main" class="main-content">

        {% include 'admin/commons/navbar.html.twig' %}
        {% include 'restricted/commons/header.html.twig' %}
        <!-- Page content -->
        <div class="container-fluid mt--7">

            <div class="row">
                <div class="col-12">
                    <!-- Preloader -->
                    {% include('public/commons/spinner.html.twig') %}
                    <!-- end of preloader -->

                    <div class="row">
                        <div class="col-xl-4 order-2 mb-5 mb-xl-0 ">
                            {% include('admin/user/partials/upload.profile.html.twig') %}
                            <hr class="my-4">
                            {% include('admin/user/partials/card.profile.html.twig') %}
                            <hr class="my-4">
                            {% include 'admin/user/partials/password.profile.html.twig' %}
                            {#
                                <hr class="my-4">
                                {% include('admin/user/partials/skills.profile.html.twig') %}
                            #}
                        </div>
                        <div class="col-xl-8 order-1">
                            <div class="card bg-secondary shadow">
                                <div class="card-header bg-white border-0">
                                    <div class="row align-items-center">
                                        <div class="col-10">
                                            <h3 class="mb-0">Profilo utente</h3>
                                        </div>
                                        <div class="col-2">
                                            <a class="btn btn-primary" href="{{ path('admin_user_index') }}">{{ 'BACK'|trans }}</a>
                                        </div>
                                    </div>
                                </div>
                                <div class="card-body">
                                    {% include 'admin/user/partials/primary.profile.html.twig' %}
                                    <hr class="my-4">
                                    {% include 'admin/user/partials/address.profile.html.twig' %}
                                    <hr class="my-4">
                                    {% include 'admin/user/partials/secondary.profile.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>

{{ form_end(form) }}
